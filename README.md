
# File: README.md

# tsl_stack

Time-series learning stack with a simple CLI.

## Layout

src/tsl/ # package (only source of truth)
tests/ # pytest
scripts/ # helper scripts (dev_env.sh, etc.)
nf_auto_runs/ # runs/logs/artifacts (ignored from git)
dashboards/ # BI assets
db/migrations/ # SQL migrations
docs/ # docs/history
